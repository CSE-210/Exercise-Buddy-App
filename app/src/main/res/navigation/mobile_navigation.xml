<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@+id/nav_matches">

    <fragment
        android:id="@+id/nav_matches"
        android:name="com.example.activeamigo.ui.matches.MatchesPage"
        android:label="@string/menu_matches"
        tools:layout="@layout/fragment_matches" />

    <fragment
        android:id="@+id/nav_calender"
        android:name="com.example.activeamigo.ui.calendar.CalendarPage"
        android:label="@string/menu_calender"
        tools:layout="@layout/fragment_calendar" />

    <fragment
        android:id="@+id/nav_preferences"
        android:name="com.example.activeamigo.ui.preferences.PreferencesPage"
        android:label="@string/menu_preferences"
        tools:layout="@layout/fragment_preferences" />

    <fragment
        android:id="@+id/nav_settings"
        android:name="com.example.activeamigo.ui.settings.SettingsPage"
        android:label="@string/menu_settings"
        tools:layout="@layout/fragment_settings" />
</navigation>